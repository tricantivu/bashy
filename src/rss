#!/usr/bin/env bash

#############################
# Author: Alejandro Cervera #
# Date: 12/02/2022          #
#############################

# Functions
usage() {
    cat << USAGE
Description: Add multiple feeds to Newsboat
Usage: rss <URL>

Examples:

rss "http://rss.cnn.com/rss/edition_world.rss" "https://rss.nytimes.com/services/xml/rss/nyt/World.xml"

rss "https://news.ycombinator.com/rss"
USAGE
}

printError() {
    printf '%b\n' "$1" >&2
}

# Global variables.
feedsFile="${HOME}/.newsboat/urls" feeds="$*"

trap '{ printError "\nScript was interrupted."; exit 1; }' SIGINT

[[ $# -eq 0 ]] && { printError "No feeds to add.\n" >&2; usage; exit 1; }
[[ -e "${feedsFile}" ]] || echo "Warning! Feeds file used by Newsboat \"${feedsFile}\" does not exist, it will be created now."

if [[ -s "${feedsFile}" ]]; then
    echo "${feeds// /$'\n'}" >> "${feedsFile}"
else
    echo "${feeds// /$'\n'}" > "${feedsFile}"
fi

trap &> /dev/null
